@model List<Category>

@foreach (var item in Model.Where(x => x.Status == true))
{
	<div class="inbox-status">
		<ul class="inbox-st-nav">
			<li><a href="/Message/Inbox?categoryId=@item.Id"><i class="@item.IconUrl"></i> @item.Name</a></li>
		</ul>
	</div>
}